# ============================================
# OpenClaw + Ollama Docker Setup — Environment
# ============================================
# Copy this to .env and edit:  cp .env.example .env
# Then run: ./setup.sh

# ---- MODEL CONFIGURATION ----
# Change this to swap models. Examples:
#   qwen3-coder:32b    (best for tool calling, ~22GB VRAM)
#   qwen3:32b          (general purpose, ~22GB VRAM)
#   glm-4.7-flash      (precise tool calling, ~24GB VRAM)
#   gpt-oss:20b        (agent-optimized, ~14GB VRAM)
#   deepseek-r1:32b    (strong reasoning, ~22GB VRAM)
#   llama3.3:70b       (needs 48GB+ VRAM)
OLLAMA_MODEL=qwen3-coder:32b

# Context window size (tokens). OpenClaw needs at least 32k.
OLLAMA_CONTEXT_WINDOW=65536

# ---- CHROME CDP (Host Browser) ----
CHROME_CDP_PORT=9222
# CHROME_PATH=/usr/bin/google-chrome

# ---- OPENCLAW SETTINGS ----
OPENCLAW_PORT=8080
AUTH_PASSWORD=changeme
AUTH_USERNAME=admin
OPENCLAW_GATEWAY_TOKEN=

# ---- TELEGRAM (optional) ----
# TELEGRAM_BOT_TOKEN=your-telegram-bot-token
# TELEGRAM_ALLOWED_USER_ID=your-telegram-user-id

# ---- OLLAMA SETTINGS ----
OLLAMA_PORT=11434
OLLAMA_GPU_LAYERS=-1
